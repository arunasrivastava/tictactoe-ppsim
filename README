# Tic Tac Toe Stochastic CRN

To install dependencies for ppsim, run:

```pip install -r ./ppsim/requirements.txt```

To install the ppsim package:

```pip install ./ppsim```

To run simulation code run:

```python3 simulation-code/*.py```